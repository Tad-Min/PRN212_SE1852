<UserControl x:Class="NguyenVanThanhDatWPF.Views.AdminOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:NguyenVanThanhDatWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- search -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Top"
                    Grid.Column="0">
                <fa:IconImage Icon="Search" Height="20" Width="20" Foreground="{StaticResource color1}"/>
                <TextBox x:Name="txtSearch"
                         Text="Search..." 
                         FontSize="13" 
                         Height="24" Width="300"
                         Background="Transparent"
                         FontWeight="Medium"
                         FontFamily="Montserrat"                            
                         Foreground="{StaticResource color1}"
                         CaretBrush="{StaticResource color1}"
                         BorderBrush="{StaticResource color1}"
                         BorderThickness="0,0,0,1"                             
                         VerticalContentAlignment="Center"
                         Margin="5,5,0,0"/>
            </StackPanel>
            <Button Grid.Column="1"                            
                    Content="Search"            
                    FontSize="12"
                    FontFamily="Montserrat"
                    Cursor="Hand"
                    BorderThickness="0"
                    Foreground="White"
                    VerticalAlignment="Top">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#773DFF"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Height="30"
                                                CornerRadius="15"
                                                Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#836EFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Order Table -->
        <Border Grid.Row="1"
                Background="#200F53"
                CornerRadius="10" 
                Padding="10"
                Margin="0,0,0,10">
            <DataGrid x:Name="dgOrders" 
                      ColumnWidth="*"
                      Background="#200F53"
                      BorderThickness="0"
                      IsReadOnly="True"
                      AutoGenerateColumns="False" SelectionChanged="dgOrders_SelectionChanged" >
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#BCBEE0"/>
                        <Setter Property="Padding" Value="10,0,0,10"/>
                        <Setter Property="FontFamily" Value="Montserrat"/>
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding OrderId}" Header="Order Id" />
                    <DataGridTextColumn Binding="{Binding CustomerId}"  Header="Customer Id"/>
                    <DataGridTextColumn Binding="{Binding EmployeeId}"  Header="Employee Id"/>
                    <DataGridTextColumn Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy}"  Header="Order Date" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- OrderDetail Table -->
        <Border Grid.Row="2"
                Background="#200F53"
                CornerRadius="10" 
                Padding="10"
                Margin="0,0,0,10">
            <DataGrid x:Name="dgOrderDetails"
                      ColumnWidth="*"
                      Background="#200F53"
                      BorderThickness="0"
                      IsReadOnly="True"
                      AutoGenerateColumns="False">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#BCBEE0"/>
                        <Setter Property="Padding" Value="10,0,0,10"/>
                        <Setter Property="FontFamily" Value="Montserrat"/>
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding OrderId}" Header="Order Id"/>
                    <DataGridTextColumn Binding="{Binding ProductId}"  Header="Product Id"/>
                    <DataGridTextColumn Binding="{Binding UnitPrice}"  Header="Unit Price"/>
                    <DataGridTextColumn Binding="{Binding Quantity}"  Header="Quantity"/>
                    <DataGridTextColumn Binding="{Binding Discount}"  Header="Discount"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <!-- CUD -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Grid.Row="3">
            <!-- Add Order -->
            <Button x:Name="btnAddOrder" Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="Add Order"
                    FontFamily="Arial"
                    FontSize="16" Click="btnAddOrder_Click">
            </Button>
            <!-- Add Order Detail -->
            <Button x:Name="btnAddOrderDetail" Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="Add Order Detail"
                    FontFamily="Arial"
                    FontSize="12" Click="btnAddOrderDetail_Click">
                      
            </Button>
            <!-- Edit Order -->
            <Button x:Name="btnEditOrder" Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="Edit Order"
                    FontFamily="Arial"
                    FontSize="16" Click="btnEditOrder_Click">

            </Button>
            <!-- Edit Order Detail -->
            <Button x:Name="btnEditOrderDetail" Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content=" Edit Order Detail"
                    FontFamily="Arial"
                    FontSize="12" Click="btnEditOrderDetail_Click">

            </Button>
            <!-- Delete Order -->
            <Button x:Name="btnDeleteOrder"
                    Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="Delete Order"
                    FontFamily="Arial"
                    FontSize="16" Click="btnDeleteOrder_Click">
            </Button>
            
            <!-- Delete Order Detail-->
            <Button x:Name="btnDeleteOrderDetail"
                    Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="DeleteOrderDetail"
                    FontFamily="Arial"
                    FontSize="12" Click="btnDeleteOrderDetail_Click">
            </Button>

        </StackPanel>
    </Grid>
</UserControl>
