<UserControl x:Class="NguyenVanThanhDatWPF.Views.AdminReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:NguyenVanThanhDatWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Bộ lọc thời gian và nút thống kê -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Start:" VerticalAlignment="Center" Foreground="{StaticResource color1}" Margin="0,0,5,0"/>
                <DatePicker x:Name="dpStartDate" Width="120" Margin="0,0,10,0"/>
                <TextBlock Text="End:" VerticalAlignment="Center" Foreground="{StaticResource color1}" Margin="0,0,5,0"/>
                <DatePicker x:Name="dpEndDate" Width="120" />
            </StackPanel>
            <Button x:Name="btnGenerate"
                    Grid.Column="3"
                    Content="Generate"
                    FontSize="12"
                    FontFamily="Montserrat"
                    Cursor="Hand"
                    BorderThickness="0"
                    Foreground="White"
                    VerticalAlignment="Center"
                    Click="btnGenerate_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#773DFF"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Height="30"
                                            CornerRadius="15"
                                            Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#836EFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Bảng thống kê -->
        <Border Grid.Row="1"
                Background="#200F53"
                CornerRadius="10" 
                Padding="10"
                Margin="0,0,0,10">
            <DataGrid x:Name="dgReport"
                      ColumnWidth="*"
                      Background="#200F53"
                      BorderThickness="0"
                      IsReadOnly="True"
                      AutoGenerateColumns="False">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#BCBEE0"/>
                        <Setter Property="Padding" Value="10,0,0,10"/>
                        <Setter Property="FontFamily" Value="Montserrat"/>
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order Id" Binding="{Binding OrderId}"/>
                    <DataGridTextColumn Header="Customer Id" Binding="{Binding CustomerId}"/>
                    <DataGridTextColumn Header="Employee Id" Binding="{Binding EmployeeId}"/>
                    <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
