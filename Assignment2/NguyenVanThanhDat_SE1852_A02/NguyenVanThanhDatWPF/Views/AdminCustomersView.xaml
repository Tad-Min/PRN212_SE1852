<UserControl x:Class="NguyenVanThanhDatWPF.Views.AdminCustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:NguyenVanThanhDatWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- search -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal"
            VerticalAlignment="Top"
            Grid.Column="0">
                <fa:IconImage Icon="Search" Height="20" Width="20" Foreground="{StaticResource color1}"/>
                <TextBox x:Name="txtSearch"
                 Text="Search..." 
                 FontSize="13" 
                 Height="24" Width="300"
                 Background="Transparent"
                 FontWeight="Medium"
                 FontFamily="Montserrat"                            
                 Foreground="{StaticResource color1}"
                 CaretBrush="{StaticResource color1}"
                 BorderBrush="{StaticResource color1}"
                 BorderThickness="0,0,0,1"                             
                 VerticalContentAlignment="Center"
                 Margin="5,5,0,0"/>
            </StackPanel>
            <Button x:Name="btnSearch"
                    Grid.Column="1"                            
                    Content="Search"            
                    FontSize="12"
                    FontFamily="Montserrat"
                    Cursor="Hand"
                    BorderThickness="0"
                    Foreground="White"
                    VerticalAlignment="Top" Click="btnSearch_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#773DFF"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Height="30"
                                        CornerRadius="15"
                                        Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#836EFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Product Table -->
        <Border Grid.Row="1"
        Background="#200F53"
        CornerRadius="10" 
        Padding="10"
        Margin="0,0,0,10">
            <DataGrid Name="dgCustomer"
                      ColumnWidth="*"
                      Background="#200F53"
                      BorderThickness="0"
                      IsReadOnly="True"
                      AutoGenerateColumns="False">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#BCBEE0"/>
                        <Setter Property="Padding" Value="10,0,0,10"/>
                        <Setter Property="FontFamily" Value="Montserrat"/>
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding CustomerId}" Header="Customer Id" HeaderStringFormat=""/>
                    <DataGridTextColumn Binding="{Binding CompanyName}"  Header="Company Name"/>
                    <DataGridTextColumn Binding="{Binding ContactName}"  Header="Contact Name"/>
                    <DataGridTextColumn Binding="{Binding ContactTitle}"  Header="Contact Title"/>
                    <DataGridTextColumn Binding="{Binding Address}"  Header="Address"/>
                    <DataGridTextColumn Binding="{Binding Phone}"  Header="Phone"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- CUD -->
        <StackPanel Orientation="Horizontal"
            HorizontalAlignment="Center"
            Grid.Row="3">
            <!-- Add Product -->
            <Button x:Name="btnAddCustomer" Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="Add Customer"
                    FontFamily="Arial"
                    FontSize="16"
                    Width="140" Click="btnAddCustomer_Click">
            </Button>

            <!-- Update Product -->
            <Button x:Name="btnUpdateCustomer" Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="Update Customer"
                    FontFamily="Arial"
                    FontSize="16"
                    Width="140" Click="btnUpdateCustomer_Click">  
            </Button>

            <!-- Delete -->
            <Button x:Name="btnDeleteCustomer"
                    Style="{StaticResource CRUDButton}"
                    Tag="{StaticResource BgColor1}"
                    Content="Delete Customer"
                    FontFamily="Arial"
                    FontSize="16"
                    Width="140" Click="btnDeleteCustomer_Click">
            </Button>
            
        </StackPanel>
    </Grid>
</UserControl>
